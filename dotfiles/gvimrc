" GUI版vimの設定
if has('gui_macvim')
    set guifont=Monaco:h16
    colorscheme torte
    set transparency=25	" 透明度を指定
    set antialias " アンチエイリアシング
    set guioptions-=T " ツールバー削除
endif

set columns=100 " 画面上の列幅を設定する
set lines=40 " 画面上の行数
set columns=120 " 画面上の列数
set cmdheight=2 " コマンドラインに使われる画面上の行数

" いつタブページのラベルを表示するかを指定する
" 0: 表示しない
" 1: 2個以上のタブページがあるときのみ表示
" 2: 常に表示
set showtabline=0

set hlsearch " 前回の検索パターンが存在するとき、それにマッチするテキストを全て強調表示する

set guioptions+=a " Visual選択で選択されたテキストが、自動的にクリップボードレジスタにコピー

